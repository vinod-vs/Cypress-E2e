/// <reference types="cypress" />
import TestFilter from '../../support/TestFilter'

// TestFilter(["stg_ms"], () => {
  describe("Verifying SM at Stage", () => {
    it("Load Stage", () => {
      cy.visit("https://adminstgsite.woolworths.com.au/Manage");
      // Do Google Oauth
    });

    // create test to submit a classic promotion
  });
// });
